CREATE DATABASE HEFESTO_BBDD;
USE HEFESTO_BBDD;
CREATE TABLE PROVEEDOR (
    id INT AUTO_INCREMENT PRIMARY KEY,
    cif VARCHAR(50) UNIQUE,
    nombre VARCHAR(255),
    alias VARCHAR(75) UNIQUE,
    direccion VARCHAR(255),
    telefono VARCHAR(50)
);

CREATE TABLE TARIFA (
    id INT AUTO_INCREMENT PRIMARY KEY,
    nombre_tarifa VARCHAR(100),
    fecha_vigencia DATE,
    total_productos INT,
    id_proveedor INT
);

CREATE TABLE PRODUCTO (
    id INT AUTO_INCREMENT PRIMARY KEY,
    sku VARCHAR(50) UNIQUE,
    nombre VARCHAR(255),
    alias VARCHAR(100),
    ean VARCHAR(50) UNIQUE,
    descripcion TEXT,
    precio_unitario DECIMAL(10,2),
    precio_total DECIMAL(10,2),
    stock INT,
    id_tarifa INT NULL
);

CREATE TABLE ESTADO_PEDIDO (
    id INT PRIMARY KEY,
    estado VARCHAR (150)
);

CREATE TABLE PEDIDO (
    id INT AUTO_INCREMENT PRIMARY KEY,
    numero VARCHAR(50) UNIQUE,
    id_estado INT
);

CREATE TABLE PRODUCTO_PEDIDO (
    id INT AUTO_INCREMENT PRIMARY KEY,
    estado VARCHAR(50),
    cantidad INT,
    nombre_producto VARCHAR(255),
    id_producto INT NULL,
    id_pedido INT
);

CREATE TABLE CLIENTE (
    id INT AUTO_INCREMENT PRIMARY KEY,
    CIF VARCHAR(50) UNIQUE,
    direccion VARCHAR(255),
    telefono VARCHAR(50),
    historico_gasto DECIMAL(12,2)
);

CREATE TABLE USUARIO (
    id INT AUTO_INCREMENT PRIMARY KEY,
    codigo VARCHAR(50) UNIQUE,
    clave BINARY(32),
    esAdmin BOOL
);

CREATE TABLE VENTA (
    id INT AUTO_INCREMENT PRIMARY KEY,
    numero VARCHAR(50) UNIQUE,
    fecha DATE,
    id_cliente INT,
    id_usuario INT
);

CREATE TABLE VENTA_PRODUCTO (
    id INT AUTO_INCREMENT PRIMARY KEY,
    precio_unitario DECIMAL(10,2),
    nombre_producto VARCHAR(255),
    cantidad INT,
    id_producto INT NULL,
    id_venta INT
);


ALTER TABLE TARIFA
ADD FOREIGN KEY (id_proveedor) REFERENCES PROVEEDOR(id);

ALTER TABLE PRODUCTO
ADD FOREIGN KEY (id_tarifa) REFERENCES TARIFA(id) 
ON DELETE SET NULL;

ALTER TABLE PEDIDO
ADD CONSTRAINT FOREIGN KEY (id_estado) REFERENCES ESTADO_PEDIDO(id);

ALTER TABLE PRODUCTO_PEDIDO
ADD CONSTRAINT FOREIGN KEY (id_producto) REFERENCES PRODUCTO(id)
ON DELETE SET NULL;

ALTER TABLE PRODUCTO_PEDIDO
ADD CONSTRAINT FOREIGN KEY (id_pedido) REFERENCES PEDIDO(id);

ALTER TABLE VENTA
ADD CONSTRAINT FOREIGN KEY (id_cliente) REFERENCES CLIENTE(id);

ALTER TABLE VENTA
ADD CONSTRAINT FOREIGN KEY (id_usuario) REFERENCES USUARIO(id);

ALTER TABLE VENTA_PRODUCTO
ADD CONSTRAINT FOREIGN KEY (id_producto) REFERENCES PRODUCTO(id)
ON DELETE SET NULL;

ALTER TABLE VENTA_PRODUCTO
ADD CONSTRAINT FOREIGN KEY (id_venta) REFERENCES VENTA(id);

INSERT INTO ESTADO_PEDIDO (id, estado)
VALUES
(1, 'pedido realizado'),
(2, 'pedido recibido parcialmente'),
(3, 'pedido recibido completamente');
